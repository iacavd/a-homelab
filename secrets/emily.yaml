# This is a TEMPLATE for Emily's secrets.
# The user must run `sops secrets/emily.yaml` on their local machine,
# replace the placeholder values, and let sops encrypt the file.
# DO NOT commit real secrets in plaintext.

nextcloud-admin-pass: "CHANGE_ME_NEXTCLOUD_ADMIN_PASSWORD"

vaultwarden-env: |
  ADMIN_TOKEN=CHANGE_ME_VERY_LONG_RANDOM_TOKEN
  SIGNUPS_ALLOWED=false

miniflux-admin: |
  admin:CHANGE_ME_STRONG_PASSWORD

microbin-password: "CHANGE_ME_MICROBIN_PASSWORD"
